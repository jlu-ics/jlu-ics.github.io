<article id="<%= post.layout %>-<%= post.slug %>" class="article article-type-<%= post.layout %>" itemprop="blogPost" itemscope itemtype="https://schema.org/BlogPosting">
  <% if (index){
       var excerpt = post.excerpt;
       if (post.excerpt) {}
       else {
         excerpt = strip_html(post.content);
       }
       %>
  <div class="card rounded">
    <%- partial('post/gallery') %>
    <div class="card-body bg-light">
			<%- partial('post/title', {class_name: 'article-title mb-0 line-clamp line-clamp-1'}) %>
			<%- partial('post/date', {class_name: 'article-date', date_format: null}) %>
			<div class="card-text mt-3 line-clamp line-clamp-2"><%- excerpt %></div>
      <hr class="hr">
      <% if (theme.excerpt_link){ %>
      <div class="article-more-link">
        <a href="<%- url_for(post.path) %>"><%= theme.excerpt_link %></a>
      </div>
      <% } %>
    </div>
  </div>
  <% } else { %>
  <% if (is_post()) { %>
  <div class="article-cover mb-5">
    <%- partial('post/gallery') %>
  </div>
  <% } %>
  <div class="article-inner">
    <% if (post.link || post.title){ %>
    <header class="article-header">
      <% if (!is_page()) { %>
      <%- partial('post/title', {class_name: 'article-title'}) %>
      <div class="article-meta">
        <%- partial('post/date', {class_name: 'article-date', date_format: null}) %>
        <%- partial('post/category') %>
      </div>
      <% } %>
    </header>
    <% } %>
    <div class="article-entry" itemprop="articleBody">
      <%- post.content %>
    </div>
    <footer class="article-footer">
      <%- partial('post/tag') %>
    </footer>
  </div>
  <% } %>
  <% if (!index){ %>
    <%- partial('post/nav') %>
  <% } %>
</article>